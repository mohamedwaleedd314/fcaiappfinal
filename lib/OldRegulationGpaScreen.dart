import 'package:flutter/material.dart';

class OldRegulationGpaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Old Regulation GPA'),
        backgroundColor: Colors.blueAccent,
      ),
      body: Center(child: Text('Old GPA Calculator')),
    );
  }
}                  // soon i will add it